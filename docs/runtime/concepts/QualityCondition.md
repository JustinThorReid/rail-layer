# Concept: QualityCondition

An item filter may be specified in two ways, either as a string which is a quality prototype name or
as a table.

**Type:** `{comparator: ComparatorString, quality: string} | string`
