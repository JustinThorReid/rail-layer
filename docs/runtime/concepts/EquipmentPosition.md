# Concept: EquipmentPosition

Position inside an equipment grid. This uses the same format as [MapPosition](runtime:MapPosition),
meaning it can be specified either with or without explicit keys.

**Type:** `{x: int32, y: int32} | (int32, int32)`
