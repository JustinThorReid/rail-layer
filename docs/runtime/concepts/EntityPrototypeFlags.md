# Concept: EntityPrototypeFlags

A set of flags. Active flags are in the dictionary as `true`, while inactive flags aren't present at
all.  By default, none of these flags are set.

**Type:** `dict[EntityPrototypeFlag, True]`
