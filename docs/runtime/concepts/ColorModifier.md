# Concept: ColorModifier

Same as [Color](runtime:Color), but red, green, blue and alpha values can be any floating point
number, without any special handling of the range [1, 255].

**Type:** `{a: float, b: float, g: float, r: float} | (float, float, float, float)`
