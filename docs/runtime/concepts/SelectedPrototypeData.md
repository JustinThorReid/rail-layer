# Concept: SelectedPrototypeData

## Fields

| Name | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `base_type` | `IDType` | no | The base type of the prototype. E.g. `"entity"`. |
| `derived_type` | `string` | no | The `type` of the prototype. E.g. `"tree"`. |
| `name` | `string` | no | The `name` of the prototype. E.g. `"tree-05"`. |
| `quality` | `string` | yes | The `name` of the quality prototype if any. E.g. `"normal"`. |
