# Concept: SpoilToTriggerResult

## Fields

| Name | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `trigger` | `array[TriggerItem]` | no | The trigger items that are run. |
| `items_per_trigger` | `uint32` | no | The trigger runs (count-in-stack / items_per_trigger) times; rounded up. |
