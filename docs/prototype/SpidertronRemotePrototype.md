# SpidertronRemotePrototype
*typename: `spidertron-remote`*
*extends: `SelectionToolPrototype`*

The [spidertron remote](https://wiki.factorio.com/Spidertron_remote). This remote can only be used
for entities of type [SpiderVehiclePrototype](prototype:SpiderVehiclePrototype).

## Properties

| Name | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `icon_color_indicator_mask` | `FileName` | yes | Color mask for the icon. This is used to show the color of the spidertron remote LEDS in the GUI. |
| `stack_size` | `1` | no | Count of items of the same name that can be stored in one inventory slot. Must be 1 when the `"not-stackable"` flag i... |
