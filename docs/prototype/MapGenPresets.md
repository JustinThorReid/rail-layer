# MapGenPresets
*typename: `map-gen-presets`*

The available map gen presets.

## Properties

| Name | Type | Optional | Description |
| ---- | ---- | -------- | ----------- |
| `type` | `'map-gen-presets'` | no |  |
| `name` | `string` | no | Name of the map gen presets. Base game uses "default". |
